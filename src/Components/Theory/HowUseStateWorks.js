import React from 'react';

const HowUseStateWorks = () => {
    return (
        <div className='theory'>
            <h1>How useState Works</h1>
            <p > useState is a Hook that allows  to have state variables in functional components. pass the initial state to this function and it returns a variable with the current state value (not necessarily the initial state) and another function to update this value.</p>
        </div>
    );
};

export default HowUseStateWorks;